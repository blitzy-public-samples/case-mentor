# Requirement: Security Architecture - Secure configuration management for API keys and credentials
# Environment Configuration
NODE_ENV=development

# Requirement: System Configuration - Database and Authentication Settings
# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_KEY=your-service-key

# Database Connection Pool Settings
DATABASE_POOL_MIN=2
DATABASE_POOL_MAX=10

# Requirement: System Configuration - AI Evaluation Service
# OpenAI Configuration
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4
OPENAI_MAX_TOKENS=2048

# Requirement: System Configuration - Payment Processing
# Stripe Configuration
STRIPE_SECRET_KEY=sk_...
STRIPE_WEBHOOK_SECRET=whsec_...

# Requirement: System Configuration - Email Service
# Resend Email Configuration
RESEND_API_KEY=re_...
EMAIL_FROM_ADDRESS=noreply@caseinterviewpractice.com

# Human Tasks:
# 1. Replace Supabase URL and keys with actual project credentials from Supabase dashboard
# 2. Set up OpenAI API key with appropriate model access and usage limits
# 3. Configure Stripe API keys and webhook secret from Stripe dashboard
# 4. Set up Resend API key and verify sending domain
# 5. Adjust database pool settings based on expected load
# 6. Review and set appropriate environment (development/staging/production)