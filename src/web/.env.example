# Supabase Configuration
# Requirement: Authentication & Security - Secure configuration for Supabase JWT-based authentication
# Required for SupabaseClient initialization in src/web/lib/supabase.ts
NEXT_PUBLIC_SUPABASE_URL=your-project-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# API Configuration
# Requirement: External Services Integration - Backend API endpoint configuration
# Must match API_CONFIG.BASE_URL in src/web/lib/api.ts
NEXT_PUBLIC_API_URL=http://localhost:3000/api

# Application Configuration
# Requirement: System Security - Secure application URL for auth callbacks
# Used for authentication redirects and OAuth provider configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Stripe Configuration
# Requirement: External Services Integration - Stripe payment processing integration
# Required for client-side Stripe.js initialization
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=your-publishable-key