# Supabase Configuration
# Secure configuration for Supabase JWT-based authentication.
# These are required for initializing the Supabase client in `src/web/lib/supabase.ts`.
NEXT_PUBLIC_SUPABASE_URL=https://mock-supabase-url.com
NEXT_PUBLIC_SUPABASE_ANON_KEY=mock-anon-key

# API Configuration
# Backend API endpoint configuration for external services integration.
# This must match the `API_CONFIG.BASE_URL` defined in `src/web/lib/api.ts`.
NEXT_PUBLIC_API_URL=http://localhost:3000/api

# Application Configuration
# Secure application URL for authentication callbacks.
# Used for authentication redirects and OAuth provider configuration.
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Stripe Configuration
# Integration for Stripe payment processing.
# Required for client-side initialization of Stripe.js.
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=mock-stripe-publishable-key
