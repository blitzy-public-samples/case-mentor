# Docker Compose Project Configuration
# Requirement: Development Environment - Container naming and isolation
COMPOSE_PROJECT_NAME=case_interview_platform

# PostgreSQL Configuration
# Requirement: Database Configuration - Local PostgreSQL settings
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your-secure-password
POSTGRES_DB=case_interview_platform
POSTGRES_PORT=5432

# Redis Configuration
# Requirement: Cache Configuration - Redis service settings
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=your-secure-redis-password
REDIS_MAX_MEMORY=2gb

# Mailhog Configuration
# Email testing service for local development
MAILHOG_SMTP_PORT=1025
MAILHOG_UI_PORT=8025

# HUMAN TASKS:
# 1. Copy this file to .env in the same directory
# 2. Replace 'your-secure-password' with a strong password for PostgreSQL
# 3. Replace 'your-secure-redis-password' with a strong password for Redis
# 4. Ensure ports 5432, 6379, 1025, and 8025 are available on your system
# 5. Adjust REDIS_MAX_MEMORY if needed based on your system resources